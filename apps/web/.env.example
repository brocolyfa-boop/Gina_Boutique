# URL de la API. En desarrollo se deja vacía: Vite hace proxy de /api a
# localhost:3000 (ver vite.config.ts), así no hay CORS.
# En producción, la URL pública del servicio de la API en Railway.
VITE_API_URL=
