{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install --include=dev && npm run build -w @gina/shared && npm run build -w @gina/web"
  },
  "deploy": {
    "startCommand": "npm start -w @gina/web",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
